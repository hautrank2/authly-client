name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy to server via SSH
        run: |
          cd /home/hautrank2/code/authly-client
          bash deploy.sh
